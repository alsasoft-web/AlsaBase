# AlsaBase Server Configuration

# Port for the HTTP and API server (Default: 8090)
PORT=8090

# JWT signing secret key for authentication tokens
JWT_SECRET=replace_with_a_secure_random_jwt_secret_in_production

# Optional: Node environment (development | production)
NODE_ENV=development
